/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용 할 수 있다.
 * 2) 특수기호는 언더스코어와 달러를 사용 할 수 있다.
 * 3) 숫자로 이름을 시작 할 수 없다.
 *    1Name, 2Hello
 * 4) 키워드는 뱐수명으로 사용 할 수 없다.
 *    var = 'var';
 */
let whale = '고래';
var $ive = '아이브';
const _yujin = '안유진';

console.log(whale);
console.log($ive);
console.log(_yuJin);

// let 1name = 'no';
// let const = 'attention';

/**
 * Naming Convention2
 * 
 * 1) camelCase -> 대부분의 언어에서 많이 사용( 첫번째 문자 소문자, 띄워쓰기 다음 문자는 대문자)
 * 2) snake_case -> 파이썬같은 언어에서 사용(띄워쓰기가 있는 경우 언더스코어 사용)
 * 3) PascalCase -> C# 마이크로소프트 계열의 언어에서 사용함
 */
//안유진
const anYuJin = '안유진';
console.log(anYuJin);